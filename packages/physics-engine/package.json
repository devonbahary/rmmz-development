{
  "name": "physics-engine",
  "version": "0.1.0",
  "description": "TypeScript 2D top-down physics engine with collision detection for circles and rectangles",
  "module": "dist/esm/index.js",
  "types": "dist/esm/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && npm run build:tsc",
    "build:tsc": "tsc --project tsconfig.build.json --module ES2020 --outDir dist/esm",
    "watch": "tsc --project tsconfig.build.json --module ES2020 --outDir dist/esm --watch",
    "clean": "rm -rf dist",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint src/**/*.ts",
    "lint:fix": "eslint src/**/*.ts --fix",
    "format": "prettier --write \"src/**/*.ts\"",
    "prepublishOnly": "npm run build && npm test"
  },
  "keywords": [
    "physics",
    "collision",
    "2d",
    "game",
    "rpg-maker",
    "typescript",
    "aabb",
    "circle",
    "rectangle"
  ],
  "author": "Devon",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.50.0",
    "jest": "^29.7.0",
    "prettier": "^3.0.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.2.0"
  }
}
